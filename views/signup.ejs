<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>signup</title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <div class="flex nav" style="justify-content: start;">
      <a href="/"> < Home </a>
    </div>
    <h1 class="flex">Sign Up Form</h1>

    <div class="container flex flex-column">
      <div class="card">
        <div class="card_title">
          <h1>Create Account</h1>
          <span>Already have an account? <a href="/a/login">Sign In</a></span>
        </div>

        <form action="/user/signup" method="post">
          <div class="form flex flex-column" style="gap: 2rem">
            <input
              type="text"
              name="name"
              id="name"
              placeholder="UserName"
              required
            />
            <input
              type="email"
              name="email"
              placeholder="example@gmail.com"
              id="email"
              required
            />
            <input
              type="password"
              name="password"
              placeholder="Password"
              id="password"
              required
            />
            <input
              type="password"
              name="confirmPassword"
              placeholder="Confirm Password"
              id="confirmPassword"
              required
            />
            <button type="submit">Sign Up</button>
          </div>
        </form>
      </div>
      <% if (locals.success) { %>
        <p style="color: lightgreen; text-align: center;"> User Sign Up Successful. </p>       
      <%} else if (locals.error) { %>
      <h3 class="error-message">Error: <%= error %></h3>
      <% } %>
    </div>
  </body>
</html>