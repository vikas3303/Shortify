<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <div class="flex nav" style="justify-content: start">
      <a href="/"> < Home </a>
    </div>
    <h1 class="flex">Login Form</h1>

    <div class="container flex flex-column">
      <div class="card">
        <div class="card_title">
          <span>Doesn't have any account? <a href="/a/signup">Sign Up</a></span>
        </div>

        <form action="/user/signin" method="post">
          <div class="form flex flex-column" style="gap: 2rem">
            <input
              type="email"
              name="email"
              placeholder="example@gmail.com"
              id="email"
              required
            />
            <input
              type="password"
              name="password"
              placeholder="Password"
              id="password"
              required
            />
            <button type="submit">Login</button>
          </div>
        </form>
      </div>
      <% if (locals.error) { %>
      <h3 class="error-message">Error: <%= error %></h3>
      <% } %>
    </div>
  </body>
</html>